/** @type {import('next').NextConfig} */
const nextConfig = {
  // Strict mode helps surface potential issues in development
  // by double-invoking certain lifecycle functions
  reactStrictMode: true,
};

module.exports = nextConfig;
